/// The options for any list table item

// The actions a user can take when they tap the more options icon on the list table item
enum ListTableItemMenuOptions {
  Call,
  Text,
  Email,
  View,
  Edit,
  Copy,
}
