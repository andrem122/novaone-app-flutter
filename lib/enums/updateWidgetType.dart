/// The types of widgets for updating information
/// Can be a calendar (date and time updates), text field (phone number, name, etc updates)
/// and a yes or no choice

enum UpdateWidgetType {
  TextField,
  CalendarField,
  BinaryField,
}
